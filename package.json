{
  "name": "mcpsuite-crm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "dev": "pnpm --filter @mcpsuite/web dev",
    "build": "pnpm --filter @mcpsuite/web build",
    "start": "pnpm --filter @mcpsuite/web start",
    "mcp:stdio": "pnpm --filter @mcpsuite/mcp stdio",
    "mcp:http": "pnpm --filter @mcpsuite/mcp http",
    "db:setup": "pnpm --filter @mcpsuite/db run setup",
    "test": "pnpm -r --workspace-concurrency=1 test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
